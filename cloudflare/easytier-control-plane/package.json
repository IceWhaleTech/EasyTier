{
  "name": "easytier-cloudflare-control-plane",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "cf-typegen": "wrangler types",
    "postinstall": "wrangler types",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler types && tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.9",
    "@cloudflare/containers": "^0.2.0"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "wrangler": "^4.78.0"
  }
}
