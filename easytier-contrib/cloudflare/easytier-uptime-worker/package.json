{
  "name": "easytier-cloudflare-uptime-worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "cargo check --target wasm32-unknown-unknown && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}

