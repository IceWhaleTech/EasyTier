{
  "name": "easytier-cloudflare-control-plane",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "cf-typegen": "wrangler types",
    "postinstall": "wrangler types",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "wrangler types && tsc --noEmit",
    "check": "npm run lint",
    "sync:instances": "node ./scripts/sync-container-instances.mjs"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.2.0",
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2",
    "wrangler": "^4.78.0"
  }
}
